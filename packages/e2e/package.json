{
  "name": "@usertour/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Real-browser tests (Playwright). Widget suite drives the SDK gallery in apps/sdk/gallery.",
  "scripts": {
    "e2e:widget": "playwright test --project=widget",
    "e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
