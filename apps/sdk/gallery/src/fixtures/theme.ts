import { defaultSettings } from '@usertour/constants';
import type { ThemeTypesSetting } from '@usertour/types';

/** The theme a fresh project starts with. */
export const defaultTheme: ThemeTypesSetting = defaultSettings;
